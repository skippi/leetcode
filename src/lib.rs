#![deny(warnings)]

mod add_two_numbers;
mod longest_substring_without_repeating_characters;
mod two_sum;
mod valid_parentheses;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
